import { GetFormArrayControlsPipe } from './get-form-array-controls.pipe';

describe('GetFormArrayControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormArrayControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
